@startuml

interface Worker {
  + number id
  + string firstName
  + string lastName
  + string job?
  + string phoneNumber?
  + number age
}

interface Construction {
  + number id
  + string name?
  + string address
  + Date endDate?
}

@enduml